# YOLOv8 전이학습 프로젝트 (Roboflow 활용)

이 프로젝트는 Roboflow에서 라벨링한 데이터를 이용해 YOLOv8 모델을 전이학습(Transfer Learning)한 과정입니다.

---

## 📌 프로젝트 개요

- **데이터 출처**: Roboflow에서 영상 업로드 및 라벨링 수행
- **모델 종류**: YOLOv8 (nano 모델 기준, yolov8n.pt)
- **학습 방식**: 전이학습 (pretrained 모델 + custom dataset)
- **목표 클래스**: 예시로 `lane`, `traffic_sign` 등

---

## 📁 폴더 구조

dataset/
├── train/
│ ├── images/ # 학습용 이미지들 (.jpg, .png 등)
│ └── labels/ # 학습용 라벨 (.txt, YOLO 포맷)
├── valid/
│ ├── images/ # 검증용 이미지
│ └── labels/ # 검증용 라벨
├── dataset.yaml # 학습 설정 파일
├── best.pt # 학습 결과 저장된 모델 (가장 성능 좋음)

yaml
복사
편집

---

## 🔧 학습 환경

- Python 3.11
- Torch 2.6.0
- Ultralytics 8.3.170

```
pip install ultralytics
```
⚙️ 학습 코드
python
복사
편집
from ultralytics import YOLO

# 1. Pretrained 모델 불러오기
model = YOLO('yolov8n.pt')


# 2. 학습 시작
model.train(
    data='/content/dataset/dataset.yaml',  # 데이터 경로
    epochs=50,                              # 학습 횟수
    imgsz=640                               # 이미지 크기
)
📄 dataset.yaml 예시
yaml
복사
편집
path: /content/dataset
train: train/images
val: valid/images
nc: 2
names: ['lane', 'traffic_sign']
키워드	설명
nc	클래스 개수
names	클래스 이름 리스트

✅ 모델 성능 평가 결과
bash
복사
편집
model.val(data='/content/dataset/dataset.yaml')
Metric	Score
mAP@0.5	0.478
mAP@0.5:0.95	0.192
Precision	0.614
Recall	0.486

모델 성능은 Roboflow 데이터셋 구성 및 라벨 정확도에 따라 달라질 수 있습니다.

🎯 예측 예시
python
복사
편집
model = YOLO('best.pt')
results = model.predict('/content/test_image.jpg')
results[0].save(filename='result.jpg')
📊 학습 결과 확인
runs/detect/train/ 디렉토리 내에 다음 파일들이 생성됩니다:

results.png: 학습 그래프

confusion_matrix.png: 클래스별 혼동 행렬

✍️ 참고
Ultralytics 공식 문서

Roboflow export 가이드

yaml
복사
편집

---

## 👉 사용 방법

1. 위 내용을 복사해서 `README.md` 파일로 저장
2. GitHub에 업로드하거나 `git add README.md` 후 커밋/푸시

필요하면 `.md`로 다운로드할 수 있게 파일도 만들어줄게. 계속 도와줄게 😎








ChatGPT에게 묻기
